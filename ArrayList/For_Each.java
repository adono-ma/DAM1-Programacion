/*
 * Click nbfs://nbhost/SystemFileSystem/Templates/Licenses/license-default.txt to change this license
 * Click nbfs://nbhost/SystemFileSystem/Templates/Classes/Class.java to edit this template
 */
package ArrayList;
public class For_Each {
  /*  
    Un for normal:
for (i <= arr.length; i++){
    sout(arr[i]);
}
    En el for each, sin embargo:
    
    for( int iterador : arr ){
    sout(iterador);
    
    aquí iterador es arr[i]
    }
    
    for( Coche iterador : arr ){
    sout(iterador.toString[]);
    
    }
    
    Vamos a usar el for en lugar del for each cuando no queramos recorrer
    todas las posiciones del array o si necesitamos la i para algo (ejs:)
    
    for (i=0; i <= arr.length; i++){
    if(i%2==0){
    sout(arr[i]);
    }
}
    
    for (i = arr.length-1 <= 0; i--){
    sout(arr[i]);
}
  
 */   
}
