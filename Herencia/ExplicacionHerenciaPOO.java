/*
 * Click nbfs://nbhost/SystemFileSystem/Templates/Licenses/license-default.txt to change this license
 * Click nbfs://nbhost/SystemFileSystem/Templates/Classes/Class.java to edit this template
 */
package Herencia;

/**
 *
 * @author ixche
 */
public class ExplicacionHerenciaPOO {
    /*
    Sirve para reutilizar
    Sirve para redefinir comportamientos:
    -especializar
    -ampliar
    
    
    Tenemos superclase(padre) y subclases (hijos)
     ***EXTENSE: significa que una clase hereda 
    
        Se suelen definir los hijos y luego lo que ves que tienen en común se suben al padre.
    
    final en un atributo significa que se ha convertido en constante y no se puede modificar
    final en una clase significa que no puede tener hijos
    final en un método no se puede sobreescribir
    
    Las clases que tienen sus métodos estáticos se hacen abstractas: se suelen utilizar para hacer librerías de métodos y se pueden 
    utilizar por ejemplo para hacer un control de entradas
    
    sobrecarga: el override que hacemos con los constructores
    
    sobreescritura: override que se hace para escribir y se pone super.loQueSea
    
    instanceOf: una clase es de su tipo propio y del padre
        if(c1 instanceOf Persona)
    
    */
}
